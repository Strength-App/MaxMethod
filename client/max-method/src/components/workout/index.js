// Barrel for the workout-domain components. Lets pages import from a single
// `components/workout` entry point as more pieces are extracted from the large
// workout pages (day.jsx, logger.jsx) in later batches.
export { default as RestTimer } from './RestTimer.jsx';
